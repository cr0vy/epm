#!/usr/bin/env python3

import sys
import os
from os.path import realpath, dirname

sys.path.insert(0, os.path.join(dirname(realpath(__file__)), "../epm"))

from epm import MainApp

app = MainApp(sys.argv)
sys.exit(app.run())

